image:
  tag: {{ .Values.imageTags.ha_frontend }}

ingress:
  main:
    enabled: true
    annotations: 
      kubernetes.io/ingress.class: nginx
    labels: 
      ingressClassName: "nginx"
    hosts:
      - host: dashboard.{{ .Values.environment_hostname }}
        paths:
          - path: /
            pathType: Prefix